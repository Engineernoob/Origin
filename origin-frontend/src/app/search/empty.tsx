// src/app/search/empty.tsx (optional helper component)
export default function EmptySearch() {
    return (
      <div className="p-10 text-center text-muted-foreground">
        Type something in the search bar to find videos.
      </div>
    );
  }